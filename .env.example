# Copy to .env and fill in a real connection string to wire up Prisma to a
# production database. The running app itself works without this — it uses
# the in-memory/localStorage mock data layer in lib/data and lib/server.
DATABASE_URL="postgresql://user:password@localhost:5432/obsidian_tread"
